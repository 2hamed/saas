apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thor.fullname" . }}-test-connection"
  labels:
    {{- include "thor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "thor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
